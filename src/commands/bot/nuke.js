const { Client, Message } = require("discord.js");

module.exports = {
    name: '',
    description: '',

    /**
     * 
     * @param {Client} bot 
     * @param {Message} message 
     * @param {String[]} parts 
     */

    async execute(bot, message, parts) {
        // TODO: MAKE FUNCTION
    }
}